# CyberKit Python Dependencies
# =============================
# CyberKit uses Python standard library ONLY - no pip install required!
# Just run: python3 cyberkit.py
#
# The tool integrates with system tools (nmap, gobuster, etc.) via subprocess.
# Make sure these tools are installed via apt:
#   sudo apt install nmap gobuster nikto sqlmap hydra john hashcat
#
# Optional dependencies (only if you want to extend the tool):
# To install optional deps on Kali Linux, use:
#   python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt
#
# requests>=2.28.0
# python-nmap>=0.7.1
# rich>=13.0.0
